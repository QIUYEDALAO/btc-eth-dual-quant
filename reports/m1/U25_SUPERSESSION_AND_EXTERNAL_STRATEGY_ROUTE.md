# U-25 Supersession and External-Strategy Route

- Status: `pre_runtime_contract_complete_pending_exact_head_review`
- U-25 status: `superseded_unexecuted_by_existing_strategy_route`
- Decision: ADR-0016

## Completed locally

The original U-25 authorization and decision report remain byte-for-byte
unchanged. A separate append-only supersession closes U-25 and all automatic
U-26+ from-scratch successors.

The fixed 20-item inventory was acquired at the exact commits specified in the
approved plan. Nineteen verified-license files are stored read-only and were
scanned as text/AST only; they were never imported. ElliotV5_SMA is retained as
metadata/hash with `unverified_not_redistributed` and no local source. Machine
evidence separates repository and file licenses; Bandtastic records Robert
Roman and its explicit MIT declaration. Source/archive/dependency identities
are independently checked.

The candidate freeze follows the fixed order and records zero IS result rows
and zero OOS decoded rows. No modification is result-selected. AST declarations
cover fixed attributes, Parameter declarations, behavior methods and position-
increase capability without importing code. Supertrend, Diamond and Heracles
remain `runtime_resolution_pending`; required external parameter files and
overrides are zero, while runtime-observed values and both runtime hashes are
null.

The unified IS protocol freezes data identity, fixed-notional portfolio,
completed-candle/next-open execution, four costs, prior-only active-universe
benchmarks, Base/CostX2 Gates and exact selection ordering. Trial accounting is
derived from append-only result manifests as `3 + selection_trial_count`; the
empty results directory yields zero. The pre-runtime root freeze byte-binds all
route contracts, and a checker constant independently anchors that root.

The remaining unified-validation defects are corrected without runtime access:
the risk-matched benchmark uses an independent fixed-50%-gross shadow return
history and emits prior-only weekly gross traces; daily MTM supports up to five
different concurrent pairs and rejects same-pair overlap or a sixth position;
the result-blind calendar is fixed at 2,191/1,533/658 full/IS/OOS days; and the
5m derivation produces 15m/1h/4h bars only from exact 3/12/48 eligible rows.
Trial descriptors now bind all contract identities and four scenarios, block
path traversal/cross-trial reuse, and cannot rescue a failed original. The DSR
reference freezes separate Base and CostX2 M1A/M1B/M1C Sharpe sequences and
labels them heterogeneous, deliberately conservative evidence rather than a
homogeneous trial population. Result envelopes now bind candidate, variant,
scenario and all root contracts; metrics, consecutive daily equity and trade
concurrency schemas fail closed. Net return, daily-MTM Sharpe, PSR and maximum
drawdown are independently recomputed from hash-bound equity within `1e-10`;
completed-trade count is reconciled to trades, and Base/CostX2 DSR is rebuilt
from daily returns plus the full frozen trial sequence. Base/CostX2 Sharpes are
derived from reconciled equity in `(first_materialized_utc, trial_id)` order;
StressA/B cannot enter DSR. The half-open
equity calendar anchors on 2020-06-30 and preserves exactly 2,191/1,533/658
full/IS/OOS returns. Benchmark lifecycle exits occur once at the current open,
before Monday rebalance, without requiring a close for an exited symbol. All
of these changes remain synthetic and result-free.

The inventory delegates calendar authority to the unified protocol instead of
mislabeling the 2020-01-01 warmup as evaluated IS. Trades are confined to
`[2020-07-01T00:00:00Z, 2024-09-11T00:00:00Z)` and may not cross the sealed OOS
boundary. A modified trial additionally requires a strictly earlier,
materialized and reconciled passing original plus a hash-complete preregistered
package whose `after_hash` is the modified executable identity.

## Hard stop

Runtime is not authorized. PR #112 exact-head review/merge is incomplete; the
pinned VPS runtime identity is unverified; runtime effective parameters are not
frozen; causal checks have not run; and at least five candidates have not yet
passed. No compatibility PASS, causal PASS, IS metric, selection or independent
performance audit is claimed. Any later resumption requires new authorization
and must preserve source hashes, candidate order and frozen set.

## Isolation

- original IS trials materialized: 0
- modified IS trials materialized: 0
- Freqtrade loads: 0
- causal validations: 0
- selection trial count: 0
- historical opened-OOS trial count: 3
- `oos_authorized=false`
- `oos_opened=false`
- `oos_runs=0`
- `oos_rows_decoded=0`
- status: `pending_explicit_unique_oos_authorization`

No API key, private endpoint, dry-run, paper/live command, order or
`execution/live` artifact was created.
